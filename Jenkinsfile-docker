pipeline {
    agent { docker {
        image 'python:3.9-slim'
    } }
    stages {
        stage('Build Docker container') {
            steps {
                sh 'python3 --version'
            }
        }
    }
}